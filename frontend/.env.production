# Deployed FastAPI backend (Render). Vercel's own environment variable for this
# project overrides this file, so change it in both places or they will drift.
VITE_API_BASE_URL=https://ml-baha.onrender.com
